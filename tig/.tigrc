bind status <F4> !sh -c “git difftool -y \”$(git rev-parse — show-toplevel)/%(file)\”"

set tab-size = 4

# Hit Enter to jump to the next hunk
bind stage <Enter> :/^@@

bind stash A ?git stash apply %(stash)


set vertical-split = false

source ~/.tigrc.colours
